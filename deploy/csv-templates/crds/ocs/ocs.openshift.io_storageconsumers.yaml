
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: storageconsumers.ocs.openshift.io
spec:
  group: ocs.openshift.io
  names:
    kind: StorageConsumer
    listKind: StorageConsumerList
    plural: storageconsumers
    singular: storageconsumer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageConsumer is the Schema for the storageconsumers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: StorageConsumerSpec defines the desired state of StorageConsumer
            properties:
              capacity:
                anyOf:
                - type: integer
                - type: string
                description: Capacity is the total quota size allocated to a consumer.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            required:
            - capacity
            type: object
          status:
            description: StorageConsumerStatus defines the observed state of StorageConsumer
            properties:
              cephObjects:
                description: CephObjects provide details of ceph created objects required
                  for external storage
                properties:
                  blockPoolName:
                    description: BlockPoolName holds the name of created ceph block
                      pool.
                    type: string
                  cephUser:
                    description: CephUser holds the name of created ceph user.
                    type: string
                type: object
              connectionDetails:
                description: ConnectionDetails holds the reference to secret containing
                  external connection details
                properties:
                  key:
                    description: The key of the secret to select from.  Must be a
                      valid secret key.
                    type: string
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                  optional:
                    description: Specify whether the Secret or its key must be defined
                    type: boolean
                required:
                - key
                type: object
              grantedCapacity:
                anyOf:
                - type: integer
                - type: string
                description: GrantedCapacity holds granted capacity value for the
                  consumer
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              state:
                description: State describes the state of StorageConsumer
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
